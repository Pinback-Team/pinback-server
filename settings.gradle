rootProject.name = 'pinback-server'

include 'domain'
include 'core'
include 'infrastructure'
include 'shared'
include 'application'
include 'api'

buildCache {
    local {
        enabled = true
    }
}

